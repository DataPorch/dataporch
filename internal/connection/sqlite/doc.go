// Package sqlite implements the SQLite connector edge.
//
// It keeps SQLite-specific import, runtime, security, discovery, query, and
// error behavior behind the connection interfaces consumed by DataPorch.
package sqlite
